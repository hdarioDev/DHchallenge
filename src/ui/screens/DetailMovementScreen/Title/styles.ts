import {StyleSheet} from 'react-native';
import {globalStyles} from '../../../shared/theme/globalStyles';

export const styles = StyleSheet.create({
  title: {
    ...globalStyles.textLargeBold,
    fontSize: 24,
    marginTop: 20,
    marginBottom: 24,
  },
});
